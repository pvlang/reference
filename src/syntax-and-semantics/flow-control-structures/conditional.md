# Conditional

## Syntax

```
<if>      ::= "if" <expression> <block>
<else-if> ::= "else" "if" <expression> <block>
<else>    ::= "else" <block>

<conditional> ::= <if> <else-if>* <else>?
```

## Semantics

Coming soon...
